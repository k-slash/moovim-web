// Controller
app.company.controller('FormController', ['$scope', '$routeParams', 'CompanyService', function ($scope, $routeParams, CompanyService) {

    if ($routeParams.id === undefined) {
        // Create
    } else {
        // Edit
    }

}]);
